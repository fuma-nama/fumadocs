/**
 * Browser build of the type-tree entry: `collapse()` runs the TypeScript compiler,
 * so it is stubbed here and the rest stays importable from client code.
 */
export * from './types';
export * from './stringify';
export * from './validator';
export * from './sampler';

openapi: 3.0.3
info:
  title: Example API
  version: 1.0.0
paths:
  /email:
    post:
      operationId: sendEmail
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: './email.yaml#/components/schemas/EmailAddress'
      responses:
        '204':
          description: Accepted

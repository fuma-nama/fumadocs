---
title: Keys
full: true
---

import { Root, API, APIInfo, APIExample, Responses, Response, ResponseTypes, ExampleResponse, TypeScriptResponse, Property, ObjectCollapsible, Requests, Request, APIPlayground } from "fumadocs-openapi/ui";

<Root baseUrl={"https://api.unkey.dev"}>

<API>

<APIInfo method={"GET"} route={"/v1/keys.getKey"}>

## Get Key

<APIPlayground authorization={{"type":"string","name":"Authorization","defaultValue":"Bearer","isRequired":true,"description":"The Authorization access token"}} method={"GET"} route={"/v1/keys.getKey"} bodyType={"json"} path={[]} query={[{"name":"keyId","type":"string","defaultValue":"key_1234","isRequired":true,"description":"The id of the key to fetch"}]} header={[]} body={undefined} schemas={{}}>

</APIPlayground>

### Authorization

<Property name={"Authorization"} type={"Bearer <token>"} required={true}>

In: `header`

</Property>

### Query Parameters

<Property name={"keyId"} type={"string"} required={true} deprecated={false}>

The id of the key to fetch

<span>Example: `"key_1234"`</span>

<span>Minimum length: `1`</span>

</Property>

| Status code | Description |
| ----------- | ----------- |
| `200` | The configuration for a single key |
| `400` | The server cannot or will not process the request due to something that is perceived to be a client error (e.g., malformed request syntax, invalid request message framing, or deceptive request routing). |
| `401` | Although the HTTP standard specifies "unauthorized", semantically this response means "unauthenticated". That is, the client must authenticate itself to get the requested response. |
| `403` | The client does not have access rights to the content; that is, it is unauthorized, so the server is refusing to give the requested resource. Unlike 401 Unauthorized, the client's identity is known to the server. |
| `404` | The server cannot find the requested resource. In the browser, this means the URL is not recognized. In an API, this can also mean that the endpoint is valid but the resource itself does not exist. Servers may also send this response instead of 403 Forbidden to hide the existence of a resource from an unauthorized client. This response code is probably the most well known due to its frequent occurrence on the web. |
| `409` | This response is sent when a request conflicts with the current state of the server. |
| `429` | The user has sent too many requests in a given amount of time ("rate limiting") |
| `500` | The server has encountered a situation it does not know how to handle. |

</APIInfo>

<APIExample>

<Requests items={["cURL","JavaScript"]}>

<Request value={"cURL"}>

```bash
curl -X GET "https://api.unkey.dev/v1/keys.getKey?keyId=key_1234"
```

</Request>

<Request value={"JavaScript"}>

```js
fetch("https://api.unkey.dev/v1/keys.getKey?keyId=key_1234", {
  method: "GET"
});
```

</Request>

</Requests>

<Responses items={["200","400","401","403","404","409","429","500"]}>

<Response value={"200"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "id": "key_1234",
  "start": "sk_5j1",
  "workspaceId": "ws_1234",
  "apiId": "api_1234",
  "name": "Customer X",
  "ownerId": "user_123",
  "meta": {
    "roles": [
      "admin",
      "user"
    ],
    "stripeCustomerId": "cus_1234"
  },
  "createdAt": 0,
  "deletedAt": 0,
  "expires": 0,
  "remaining": 1000,
  "refill": {
    "interval": "daily",
    "amount": 10
  },
  "ratelimit": {
    "type": "fast",
    "limit": 10,
    "refillRate": 1,
    "refillInterval": 60
  },
  "roles": [
    "admin",
    "finance"
  ],
  "enabled": true
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  /**
   * The id of the key
   */
  id: string;
  /**
   * The first few characters of the key to visually identify it
   */
  start: string;
  /**
   * The id of the workspace that owns the key
   */
  workspaceId: string;
  /**
   * The id of the api that this key is for
   */
  apiId?: string;
  /**
   * The name of the key, give keys a name to easily identify their purpose
   */
  name?: string;
  /**
   * The id of the tenant associated with this key. Use whatever reference you have in your system to identify the tenant. When verifying the key, we will send this field back to you, so you know who is accessing your API.
   */
  ownerId?: string;
  /**
   * Any additional metadata you want to store with the key
   */
  meta?: {
    [k: string]: {
      [k: string]: unknown;
    };
  };
  /**
   * The unix timestamp in milliseconds when the key was created
   */
  createdAt?: number;
  /**
   * The unix timestamp in milliseconds when the key was deleted. We don't delete the key outright, you can restore it later.
   */
  deletedAt?: number;
  /**
   * The unix timestamp in milliseconds when the key will expire. If this field is null or undefined, the key is not expiring.
   */
  expires?: number;
  /**
   * The number of requests that can be made with this key before it becomes invalid. If this field is null or undefined, the key has no request limit.
   */
  remaining?: number;
  /**
   * Unkey allows you to refill remaining verifications on a key on a regular interval.
   */
  refill?: {
    /**
     * Determines the rate at which verifications will be refilled.
     */
    interval: "daily" | "monthly";
    /**
     * Resets `remaining` to this value every interval.
     */
    amount: number;
    /**
     * The unix timestamp in miliseconds when the key was last refilled.
     */
    lastRefillAt?: number;
  };
  /**
   * Unkey comes with per-key ratelimiting out of the box.
   */
  ratelimit?: {
    /**
     * Fast ratelimiting doesn't add latency, while consistent ratelimiting is more accurate.
     */
    type: "fast" | "consistent";
    /**
     * The total amount of burstable requests.
     */
    limit: number;
    /**
     * How many tokens to refill during each refillInterval.
     */
    refillRate: number;
    /**
     * Determines the speed at which tokens are refilled, in milliseconds.
     */
    refillInterval: number;
  };
  /**
   * All roles this key belongs to
   */
  roles?: string[];
  /**
   * Sets if key is enabled or disabled. Disabled keys are not valid.
   */
  enabled?: boolean;
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"400"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "error": {
    "code": "BAD_REQUEST",
    "docs": "https://unkey.dev/docs/api-reference/errors/code/BAD_REQUEST",
    "message": "string",
    "requestId": "req_1234"
  }
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  error: {
    /**
     * A machine readable error code.
     */
    code: "BAD_REQUEST";
    /**
     * A link to our documentation with more details about this error code
     */
    docs: string;
    /**
     * A human readable explanation of what went wrong
     */
    message: string;
    /**
     * Please always include the requestId in your error report
     */
    requestId: string;
  };
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"401"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "error": {
    "code": "UNAUTHORIZED",
    "docs": "https://unkey.dev/docs/api-reference/errors/code/UNAUTHORIZED",
    "message": "string",
    "requestId": "req_1234"
  }
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  error: {
    /**
     * A machine readable error code.
     */
    code: "UNAUTHORIZED";
    /**
     * A link to our documentation with more details about this error code
     */
    docs: string;
    /**
     * A human readable explanation of what went wrong
     */
    message: string;
    /**
     * Please always include the requestId in your error report
     */
    requestId: string;
  };
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"403"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "error": {
    "code": "FORBIDDEN",
    "docs": "https://unkey.dev/docs/api-reference/errors/code/FORBIDDEN",
    "message": "string",
    "requestId": "req_1234"
  }
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  error: {
    /**
     * A machine readable error code.
     */
    code: "FORBIDDEN";
    /**
     * A link to our documentation with more details about this error code
     */
    docs: string;
    /**
     * A human readable explanation of what went wrong
     */
    message: string;
    /**
     * Please always include the requestId in your error report
     */
    requestId: string;
  };
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"404"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "error": {
    "code": "NOT_FOUND",
    "docs": "https://unkey.dev/docs/api-reference/errors/code/NOT_FOUND",
    "message": "string",
    "requestId": "req_1234"
  }
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  error: {
    /**
     * A machine readable error code.
     */
    code: "NOT_FOUND";
    /**
     * A link to our documentation with more details about this error code
     */
    docs: string;
    /**
     * A human readable explanation of what went wrong
     */
    message: string;
    /**
     * Please always include the requestId in your error report
     */
    requestId: string;
  };
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"409"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "error": {
    "code": "CONFLICT",
    "docs": "https://unkey.dev/docs/api-reference/errors/code/CONFLICT",
    "message": "string",
    "requestId": "req_1234"
  }
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  error: {
    /**
     * A machine readable error code.
     */
    code: "CONFLICT";
    /**
     * A link to our documentation with more details about this error code
     */
    docs: string;
    /**
     * A human readable explanation of what went wrong
     */
    message: string;
    /**
     * Please always include the requestId in your error report
     */
    requestId: string;
  };
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"429"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "error": {
    "code": "TOO_MANY_REQUESTS",
    "docs": "https://unkey.dev/docs/api-reference/errors/code/TOO_MANY_REQUESTS",
    "message": "string",
    "requestId": "req_1234"
  }
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  error: {
    /**
     * A machine readable error code.
     */
    code: "TOO_MANY_REQUESTS";
    /**
     * A link to our documentation with more details about this error code
     */
    docs: string;
    /**
     * A human readable explanation of what went wrong
     */
    message: string;
    /**
     * Please always include the requestId in your error report
     */
    requestId: string;
  };
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"500"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "error": {
    "code": "INTERNAL_SERVER_ERROR",
    "docs": "https://unkey.dev/docs/api-reference/errors/code/INTERNAL_SERVER_ERROR",
    "message": "string",
    "requestId": "req_1234"
  }
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  error: {
    /**
     * A machine readable error code.
     */
    code: "INTERNAL_SERVER_ERROR";
    /**
     * A link to our documentation with more details about this error code
     */
    docs: string;
    /**
     * A human readable explanation of what went wrong
     */
    message: string;
    /**
     * Please always include the requestId in your error report
     */
    requestId: string;
  };
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

</Responses>

</APIExample>

</API>

<API>

<APIInfo method={"POST"} route={"/v1/keys.deleteKey"}>

## Delete Key

<APIPlayground authorization={{"type":"string","name":"Authorization","defaultValue":"Bearer","isRequired":true,"description":"The Authorization access token"}} method={"POST"} route={"/v1/keys.deleteKey"} bodyType={"json"} path={[]} query={[]} header={[]} body={{"type":"object","isRequired":true,"properties":{"keyId":{"type":"ref","isRequired":true,"schema":"0"}}}} schemas={{"0":{"type":"string","defaultValue":"key_1234","isRequired":false,"description":"The id of the key to revoke"}}}>

</APIPlayground>

### Authorization

<Property name={"Authorization"} type={"Bearer <token>"} required={true}>

In: `header`

</Property>

### Request Body

<Property name={"keyId"} type={"string"} required={true} deprecated={undefined}>

The id of the key to revoke

<span>Example: `"key_1234"`</span>

<span>Minimum length: `1`</span>

</Property>

| Status code | Description |
| ----------- | ----------- |
| `200` | The key was successfully revoked, it may take up to 30s for this to take effect in all regions |
| `400` | The server cannot or will not process the request due to something that is perceived to be a client error (e.g., malformed request syntax, invalid request message framing, or deceptive request routing). |
| `401` | Although the HTTP standard specifies "unauthorized", semantically this response means "unauthenticated". That is, the client must authenticate itself to get the requested response. |
| `403` | The client does not have access rights to the content; that is, it is unauthorized, so the server is refusing to give the requested resource. Unlike 401 Unauthorized, the client's identity is known to the server. |
| `404` | The server cannot find the requested resource. In the browser, this means the URL is not recognized. In an API, this can also mean that the endpoint is valid but the resource itself does not exist. Servers may also send this response instead of 403 Forbidden to hide the existence of a resource from an unauthorized client. This response code is probably the most well known due to its frequent occurrence on the web. |
| `409` | This response is sent when a request conflicts with the current state of the server. |
| `429` | The user has sent too many requests in a given amount of time ("rate limiting") |
| `500` | The server has encountered a situation it does not know how to handle. |

</APIInfo>

<APIExample>

<Requests items={["cURL","JavaScript"]}>

<Request value={"cURL"}>

```bash
curl -X POST "https://api.unkey.dev/v1/keys.deleteKey" \
  -H "Content-Type: application/json" \
  -d '{
    "keyId": "key_1234"
  }'
```

</Request>

<Request value={"JavaScript"}>

```js
fetch("https://api.unkey.dev/v1/keys.deleteKey", {
  method: "POST",
  body: JSON.stringify({
    "keyId": "key_1234"
  })
});
```

</Request>

</Requests>

<Responses items={["200","400","401","403","404","409","429","500"]}>

<Response value={"200"}>

<ResponseTypes>

<ExampleResponse>

```json
{}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"400"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "error": {
    "code": "BAD_REQUEST",
    "docs": "https://unkey.dev/docs/api-reference/errors/code/BAD_REQUEST",
    "message": "string",
    "requestId": "req_1234"
  }
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  error: {
    /**
     * A machine readable error code.
     */
    code: "BAD_REQUEST";
    /**
     * A link to our documentation with more details about this error code
     */
    docs: string;
    /**
     * A human readable explanation of what went wrong
     */
    message: string;
    /**
     * Please always include the requestId in your error report
     */
    requestId: string;
  };
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"401"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "error": {
    "code": "UNAUTHORIZED",
    "docs": "https://unkey.dev/docs/api-reference/errors/code/UNAUTHORIZED",
    "message": "string",
    "requestId": "req_1234"
  }
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  error: {
    /**
     * A machine readable error code.
     */
    code: "UNAUTHORIZED";
    /**
     * A link to our documentation with more details about this error code
     */
    docs: string;
    /**
     * A human readable explanation of what went wrong
     */
    message: string;
    /**
     * Please always include the requestId in your error report
     */
    requestId: string;
  };
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"403"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "error": {
    "code": "FORBIDDEN",
    "docs": "https://unkey.dev/docs/api-reference/errors/code/FORBIDDEN",
    "message": "string",
    "requestId": "req_1234"
  }
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  error: {
    /**
     * A machine readable error code.
     */
    code: "FORBIDDEN";
    /**
     * A link to our documentation with more details about this error code
     */
    docs: string;
    /**
     * A human readable explanation of what went wrong
     */
    message: string;
    /**
     * Please always include the requestId in your error report
     */
    requestId: string;
  };
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"404"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "error": {
    "code": "NOT_FOUND",
    "docs": "https://unkey.dev/docs/api-reference/errors/code/NOT_FOUND",
    "message": "string",
    "requestId": "req_1234"
  }
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  error: {
    /**
     * A machine readable error code.
     */
    code: "NOT_FOUND";
    /**
     * A link to our documentation with more details about this error code
     */
    docs: string;
    /**
     * A human readable explanation of what went wrong
     */
    message: string;
    /**
     * Please always include the requestId in your error report
     */
    requestId: string;
  };
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"409"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "error": {
    "code": "CONFLICT",
    "docs": "https://unkey.dev/docs/api-reference/errors/code/CONFLICT",
    "message": "string",
    "requestId": "req_1234"
  }
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  error: {
    /**
     * A machine readable error code.
     */
    code: "CONFLICT";
    /**
     * A link to our documentation with more details about this error code
     */
    docs: string;
    /**
     * A human readable explanation of what went wrong
     */
    message: string;
    /**
     * Please always include the requestId in your error report
     */
    requestId: string;
  };
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"429"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "error": {
    "code": "TOO_MANY_REQUESTS",
    "docs": "https://unkey.dev/docs/api-reference/errors/code/TOO_MANY_REQUESTS",
    "message": "string",
    "requestId": "req_1234"
  }
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  error: {
    /**
     * A machine readable error code.
     */
    code: "TOO_MANY_REQUESTS";
    /**
     * A link to our documentation with more details about this error code
     */
    docs: string;
    /**
     * A human readable explanation of what went wrong
     */
    message: string;
    /**
     * Please always include the requestId in your error report
     */
    requestId: string;
  };
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"500"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "error": {
    "code": "INTERNAL_SERVER_ERROR",
    "docs": "https://unkey.dev/docs/api-reference/errors/code/INTERNAL_SERVER_ERROR",
    "message": "string",
    "requestId": "req_1234"
  }
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  error: {
    /**
     * A machine readable error code.
     */
    code: "INTERNAL_SERVER_ERROR";
    /**
     * A link to our documentation with more details about this error code
     */
    docs: string;
    /**
     * A human readable explanation of what went wrong
     */
    message: string;
    /**
     * Please always include the requestId in your error report
     */
    requestId: string;
  };
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

</Responses>

</APIExample>

</API>

<API>

<APIInfo method={"POST"} route={"/v1/keys.createKey"}>

## Create Key

<APIPlayground authorization={{"type":"string","name":"Authorization","defaultValue":"Bearer","isRequired":true,"description":"The Authorization access token"}} method={"POST"} route={"/v1/keys.createKey"} bodyType={"json"} path={[]} query={[]} header={[]} body={{"type":"object","isRequired":true,"properties":{"apiId":{"type":"ref","isRequired":true,"schema":"0"},"prefix":{"type":"ref","isRequired":false,"schema":"1"},"name":{"type":"ref","isRequired":false,"schema":"2"},"byteLength":{"type":"ref","isRequired":false,"schema":"3"},"ownerId":{"type":"ref","isRequired":false,"schema":"4"},"meta":{"type":"ref","isRequired":false,"schema":"5"},"roles":{"type":"ref","isRequired":false,"schema":"6"},"expires":{"type":"ref","isRequired":false,"schema":"8"},"remaining":{"type":"ref","isRequired":false,"schema":"9"},"refill":{"type":"ref","isRequired":false,"schema":"10"},"ratelimit":{"type":"ref","isRequired":false,"schema":"13"},"enabled":{"type":"ref","isRequired":false,"schema":"18"}}}} schemas={{"0":{"type":"string","defaultValue":"api_123","isRequired":false,"description":"Choose an `API` where this key should be created."},"1":{"type":"string","defaultValue":"","isRequired":false,"description":"To make it easier for your users to understand which product an api key belongs to, you can add prefix them.\n\nFor example Stripe famously prefixes their customer ids with cus_ or their api keys with sk_live_.\n\nThe underscore is automatically added if you are defining a prefix, for example: \"prefix\": \"abc\" will result in a key like abc_xxxxxxxxx\n"},"2":{"type":"string","defaultValue":"my key","isRequired":false,"description":"The name for your Key. This is not customer facing."},"3":{"type":"number","defaultValue":"","isRequired":false,"description":"The byte length used to generate your key determines its entropy as well as its length. Higher is better, but keys become longer and more annoying to handle. The default is 16 bytes, or 2^^128 possible combinations."},"4":{"type":"string","defaultValue":"team_123","isRequired":false,"description":"Your user’s Id. This will provide a link between Unkey and your customer record.\nWhen validating a key, we will return this back to you, so you can clearly identify your user from their api key."},"5":{"type":"object","isRequired":false,"description":"This is a place for dynamic meta data, anything that feels useful for you should go here","properties":{},"additionalProperties":true},"6":{"type":"array","description":"A list of roles that this key should have. New roles will be created if they don't exist yet.","isRequired":false,"items":"7"},"7":{"type":"string","defaultValue":"","isRequired":false},"8":{"type":"number","defaultValue":1623869797161,"isRequired":false,"description":"You can auto expire keys by providing a unix timestamp in milliseconds. Once Keys expire they will automatically be disabled and are no longer valid unless you enable them again."},"9":{"type":"number","defaultValue":1000,"isRequired":false,"description":"You can limit the number of requests a key can make. Once a key reaches 0 remaining requests, it will automatically be disabled and is no longer valid unless you update it."},"10":{"type":"object","isRequired":false,"description":"Unkey enables you to refill verifications for each key at regular intervals.","properties":{"interval":{"type":"ref","isRequired":true,"schema":"11"},"amount":{"type":"ref","isRequired":true,"schema":"12"}}},"11":{"type":"string","defaultValue":"","isRequired":false,"description":"Unkey will automatically refill verifications at the set interval."},"12":{"type":"number","defaultValue":"","isRequired":false,"description":"The number of verifications to refill for each occurrence is determined individually for each key."},"13":{"type":"object","isRequired":false,"description":"Unkey comes with per-key ratelimiting out of the box.","properties":{"type":{"type":"ref","isRequired":true,"schema":"14"},"limit":{"type":"ref","isRequired":true,"schema":"15"},"refillRate":{"type":"ref","isRequired":true,"schema":"16"},"refillInterval":{"type":"ref","isRequired":true,"schema":"17"}}},"14":{"type":"string","defaultValue":"","isRequired":false,"description":"Fast ratelimiting doesn't add latency, while consistent ratelimiting is more accurate."},"15":{"type":"number","defaultValue":"","isRequired":false,"description":"The total amount of burstable requests."},"16":{"type":"number","defaultValue":"","isRequired":false,"description":"How many tokens to refill during each refillInterval."},"17":{"type":"number","defaultValue":"","isRequired":false,"description":"Determines the speed at which tokens are refilled, in milliseconds."},"18":{"type":"boolean","defaultValue":false,"isRequired":false,"description":"Sets if key is enabled or disabled. Disabled keys are not valid."}}}>

</APIPlayground>

### Authorization

<Property name={"Authorization"} type={"Bearer <token>"} required={true}>

In: `header`

</Property>

### Request Body

<Property name={"apiId"} type={"string"} required={true} deprecated={undefined}>

Choose an `API` where this key should be created.

<span>Example: `"api_123"`</span>

</Property>

<Property name={"prefix"} type={"string"} required={false} deprecated={undefined}>

To make it easier for your users to understand which product an api key belongs to, you can add prefix them.

For example Stripe famously prefixes their customer ids with cus_ or their api keys with sk_live_.

The underscore is automatically added if you are defining a prefix, for example: "prefix": "abc" will result in a key like abc_xxxxxxxxx


<span>Maximum length: `8`</span>

</Property>

<Property name={"name"} type={"string"} required={false} deprecated={undefined}>

The name for your Key. This is not customer facing.

<span>Example: `"my key"`</span>

</Property>

<Property name={"byteLength"} type={"integer"} required={false} deprecated={undefined}>

The byte length used to generate your key determines its entropy as well as its length. Higher is better, but keys become longer and more annoying to handle. The default is 16 bytes, or 2^^128 possible combinations.

<span>Default: `16`</span>

<span>Minimum: `16`</span>

<span>Maximum: `255`</span>

</Property>

<Property name={"ownerId"} type={"string"} required={false} deprecated={undefined}>

Your user’s Id. This will provide a link between Unkey and your customer record.
When validating a key, we will return this back to you, so you can clearly identify your user from their api key.

<span>Example: `"team_123"`</span>

</Property>

<Property name={"meta"} type={"object"} required={false} deprecated={undefined}>

This is a place for dynamic meta data, anything that feels useful for you should go here

<span>Example: `{"billingTier":"PRO","trialEnds":"2023-06-16T17:16:37.161Z"}`</span>

<ObjectCollapsible name={"Attributes"}>

<Property name={"[key: string]"} type={"null"} required={false} deprecated={undefined}>

</Property>

</ObjectCollapsible>

</Property>

<Property name={"roles"} type={"array<string>"} required={false} deprecated={undefined}>

A list of roles that this key should have. New roles will be created if they don't exist yet.

<span>Example: `["admin","finance"]`</span>

</Property>

<Property name={"expires"} type={"integer"} required={false} deprecated={undefined}>

You can auto expire keys by providing a unix timestamp in milliseconds. Once Keys expire they will automatically be disabled and are no longer valid unless you enable them again.

<span>Example: `1623869797161`</span>

</Property>

<Property name={"remaining"} type={"integer"} required={false} deprecated={undefined}>

You can limit the number of requests a key can make. Once a key reaches 0 remaining requests, it will automatically be disabled and is no longer valid unless you update it.

<span>Example: `1000`</span>

</Property>

<Property name={"refill"} type={"object"} required={false} deprecated={undefined}>

Unkey enables you to refill verifications for each key at regular intervals.

<span>Example: `{"interval":"daily","amount":100}`</span>

<ObjectCollapsible name={"Attributes"}>

<Property name={"interval"} type={"string"} required={true} deprecated={undefined}>

Unkey will automatically refill verifications at the set interval.

<span>Value in: `"daily" | "monthly"`</span>

</Property>

<Property name={"amount"} type={"integer"} required={true} deprecated={undefined}>

The number of verifications to refill for each occurrence is determined individually for each key.

<span>Minimum: `0`</span>

</Property>

</ObjectCollapsible>

</Property>

<Property name={"ratelimit"} type={"object"} required={false} deprecated={undefined}>

Unkey comes with per-key ratelimiting out of the box.

<span>Example: `{"type":"fast","limit":10,"refillRate":1,"refillInterval":60}`</span>

<ObjectCollapsible name={"Attributes"}>

<Property name={"type"} type={"string"} required={true} deprecated={undefined}>

Fast ratelimiting doesn't add latency, while consistent ratelimiting is more accurate.

<span>Default: `"fast"`</span>

<span>Value in: `"fast" | "consistent"`</span>

</Property>

<Property name={"limit"} type={"integer"} required={true} deprecated={undefined}>

The total amount of burstable requests.

<span>Minimum: `1`</span>

</Property>

<Property name={"refillRate"} type={"integer"} required={true} deprecated={undefined}>

How many tokens to refill during each refillInterval.

<span>Minimum: `1`</span>

</Property>

<Property name={"refillInterval"} type={"integer"} required={true} deprecated={undefined}>

Determines the speed at which tokens are refilled, in milliseconds.

<span>Minimum: `1`</span>

</Property>

</ObjectCollapsible>

</Property>

<Property name={"enabled"} type={"boolean"} required={false} deprecated={undefined}>

Sets if key is enabled or disabled. Disabled keys are not valid.

<span>Example: `false`</span>

<span>Default: `true`</span>

</Property>

| Status code | Description |
| ----------- | ----------- |
| `200` | The configuration for an api |
| `400` | The server cannot or will not process the request due to something that is perceived to be a client error (e.g., malformed request syntax, invalid request message framing, or deceptive request routing). |
| `401` | Although the HTTP standard specifies "unauthorized", semantically this response means "unauthenticated". That is, the client must authenticate itself to get the requested response. |
| `403` | The client does not have access rights to the content; that is, it is unauthorized, so the server is refusing to give the requested resource. Unlike 401 Unauthorized, the client's identity is known to the server. |
| `404` | The server cannot find the requested resource. In the browser, this means the URL is not recognized. In an API, this can also mean that the endpoint is valid but the resource itself does not exist. Servers may also send this response instead of 403 Forbidden to hide the existence of a resource from an unauthorized client. This response code is probably the most well known due to its frequent occurrence on the web. |
| `409` | This response is sent when a request conflicts with the current state of the server. |
| `429` | The user has sent too many requests in a given amount of time ("rate limiting") |
| `500` | The server has encountered a situation it does not know how to handle. |

</APIInfo>

<APIExample>

<Requests items={["cURL","JavaScript"]}>

<Request value={"cURL"}>

```bash
curl -X POST "https://api.unkey.dev/v1/keys.createKey" \
  -H "Content-Type: application/json" \
  -d '{
    "apiId": "api_123",
    "prefix": "string",
    "name": "my key",
    "byteLength": 16,
    "ownerId": "team_123",
    "meta": {
      "billingTier": "PRO",
      "trialEnds": "2023-06-16T17:16:37.161Z"
    },
    "roles": [
      "admin",
      "finance"
    ],
    "expires": 1623869797161,
    "remaining": 1000,
    "refill": {
      "interval": "daily",
      "amount": 100
    },
    "ratelimit": {
      "type": "fast",
      "limit": 10,
      "refillRate": 1,
      "refillInterval": 60
    },
    "enabled": false
  }'
```

</Request>

<Request value={"JavaScript"}>

```js
fetch("https://api.unkey.dev/v1/keys.createKey", {
  method: "POST",
  body: JSON.stringify({
    "apiId": "api_123",
    "prefix": "string",
    "name": "my key",
    "byteLength": 16,
    "ownerId": "team_123",
    "meta": {
      "billingTier": "PRO",
      "trialEnds": "2023-06-16T17:16:37.161Z"
    },
    "roles": [
      "admin",
      "finance"
    ],
    "expires": 1623869797161,
    "remaining": 1000,
    "refill": {
      "interval": "daily",
      "amount": 100
    },
    "ratelimit": {
      "type": "fast",
      "limit": 10,
      "refillRate": 1,
      "refillInterval": 60
    },
    "enabled": false
  })
});
```

</Request>

</Requests>

<Responses items={["200","400","401","403","404","409","429","500"]}>

<Response value={"200"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "keyId": "key_123",
  "key": "prefix_xxxxxxxxx"
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  /**
   * The id of the key. This is not a secret and can be stored as a reference if you wish. You need the keyId to update or delete a key later.
   */
  keyId: string;
  /**
   * The newly created api key, do not store this on your own system but pass it along to your user.
   */
  key: string;
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"400"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "error": {
    "code": "BAD_REQUEST",
    "docs": "https://unkey.dev/docs/api-reference/errors/code/BAD_REQUEST",
    "message": "string",
    "requestId": "req_1234"
  }
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  error: {
    /**
     * A machine readable error code.
     */
    code: "BAD_REQUEST";
    /**
     * A link to our documentation with more details about this error code
     */
    docs: string;
    /**
     * A human readable explanation of what went wrong
     */
    message: string;
    /**
     * Please always include the requestId in your error report
     */
    requestId: string;
  };
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"401"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "error": {
    "code": "UNAUTHORIZED",
    "docs": "https://unkey.dev/docs/api-reference/errors/code/UNAUTHORIZED",
    "message": "string",
    "requestId": "req_1234"
  }
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  error: {
    /**
     * A machine readable error code.
     */
    code: "UNAUTHORIZED";
    /**
     * A link to our documentation with more details about this error code
     */
    docs: string;
    /**
     * A human readable explanation of what went wrong
     */
    message: string;
    /**
     * Please always include the requestId in your error report
     */
    requestId: string;
  };
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"403"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "error": {
    "code": "FORBIDDEN",
    "docs": "https://unkey.dev/docs/api-reference/errors/code/FORBIDDEN",
    "message": "string",
    "requestId": "req_1234"
  }
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  error: {
    /**
     * A machine readable error code.
     */
    code: "FORBIDDEN";
    /**
     * A link to our documentation with more details about this error code
     */
    docs: string;
    /**
     * A human readable explanation of what went wrong
     */
    message: string;
    /**
     * Please always include the requestId in your error report
     */
    requestId: string;
  };
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"404"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "error": {
    "code": "NOT_FOUND",
    "docs": "https://unkey.dev/docs/api-reference/errors/code/NOT_FOUND",
    "message": "string",
    "requestId": "req_1234"
  }
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  error: {
    /**
     * A machine readable error code.
     */
    code: "NOT_FOUND";
    /**
     * A link to our documentation with more details about this error code
     */
    docs: string;
    /**
     * A human readable explanation of what went wrong
     */
    message: string;
    /**
     * Please always include the requestId in your error report
     */
    requestId: string;
  };
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"409"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "error": {
    "code": "CONFLICT",
    "docs": "https://unkey.dev/docs/api-reference/errors/code/CONFLICT",
    "message": "string",
    "requestId": "req_1234"
  }
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  error: {
    /**
     * A machine readable error code.
     */
    code: "CONFLICT";
    /**
     * A link to our documentation with more details about this error code
     */
    docs: string;
    /**
     * A human readable explanation of what went wrong
     */
    message: string;
    /**
     * Please always include the requestId in your error report
     */
    requestId: string;
  };
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"429"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "error": {
    "code": "TOO_MANY_REQUESTS",
    "docs": "https://unkey.dev/docs/api-reference/errors/code/TOO_MANY_REQUESTS",
    "message": "string",
    "requestId": "req_1234"
  }
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  error: {
    /**
     * A machine readable error code.
     */
    code: "TOO_MANY_REQUESTS";
    /**
     * A link to our documentation with more details about this error code
     */
    docs: string;
    /**
     * A human readable explanation of what went wrong
     */
    message: string;
    /**
     * Please always include the requestId in your error report
     */
    requestId: string;
  };
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"500"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "error": {
    "code": "INTERNAL_SERVER_ERROR",
    "docs": "https://unkey.dev/docs/api-reference/errors/code/INTERNAL_SERVER_ERROR",
    "message": "string",
    "requestId": "req_1234"
  }
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  error: {
    /**
     * A machine readable error code.
     */
    code: "INTERNAL_SERVER_ERROR";
    /**
     * A link to our documentation with more details about this error code
     */
    docs: string;
    /**
     * A human readable explanation of what went wrong
     */
    message: string;
    /**
     * Please always include the requestId in your error report
     */
    requestId: string;
  };
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

</Responses>

</APIExample>

</API>

<API>

<APIInfo method={"POST"} route={"/v1/keys.verifyKey"}>

## Verify Key

<APIPlayground authorization={undefined} method={"POST"} route={"/v1/keys.verifyKey"} bodyType={"json"} path={[]} query={[]} header={[]} body={{"type":"object","isRequired":true,"properties":{"apiId":{"type":"ref","isRequired":false,"schema":"0"},"key":{"type":"ref","isRequired":true,"schema":"1"}}}} schemas={{"0":{"type":"string","defaultValue":"api_1234","isRequired":false,"description":"The id of the api where the key belongs to. This is optional for now but will be required soon.\nThe key will be verified against the api's configuration. If the key does not belong to the api, the verification will fail."},"1":{"type":"string","defaultValue":"sk_1234","isRequired":false,"description":"The key to verify"}}}>

</APIPlayground>

### Request Body

<Property name={"apiId"} type={"string"} required={false} deprecated={undefined}>

The id of the api where the key belongs to. This is optional for now but will be required soon.
The key will be verified against the api's configuration. If the key does not belong to the api, the verification will fail.

<span>Example: `"api_1234"`</span>

</Property>

<Property name={"key"} type={"string"} required={true} deprecated={undefined}>

The key to verify

<span>Example: `"sk_1234"`</span>

<span>Minimum length: `1`</span>

</Property>

| Status code | Description |
| ----------- | ----------- |
| `200` | The verification result |
| `400` | The server cannot or will not process the request due to something that is perceived to be a client error (e.g., malformed request syntax, invalid request message framing, or deceptive request routing). |
| `401` | Although the HTTP standard specifies "unauthorized", semantically this response means "unauthenticated". That is, the client must authenticate itself to get the requested response. |
| `403` | The client does not have access rights to the content; that is, it is unauthorized, so the server is refusing to give the requested resource. Unlike 401 Unauthorized, the client's identity is known to the server. |
| `404` | The server cannot find the requested resource. In the browser, this means the URL is not recognized. In an API, this can also mean that the endpoint is valid but the resource itself does not exist. Servers may also send this response instead of 403 Forbidden to hide the existence of a resource from an unauthorized client. This response code is probably the most well known due to its frequent occurrence on the web. |
| `409` | This response is sent when a request conflicts with the current state of the server. |
| `429` | The user has sent too many requests in a given amount of time ("rate limiting") |
| `500` | The server has encountered a situation it does not know how to handle. |

</APIInfo>

<APIExample>

<Requests items={["cURL","JavaScript"]}>

<Request value={"cURL"}>

```bash
curl -X POST "https://api.unkey.dev/v1/keys.verifyKey" \
  -H "Content-Type: application/json" \
  -d '{
    "apiId": "api_1234",
    "key": "sk_1234"
  }'
```

</Request>

<Request value={"JavaScript"}>

```js
fetch("https://api.unkey.dev/v1/keys.verifyKey", {
  method: "POST",
  body: JSON.stringify({
    "apiId": "api_1234",
    "key": "sk_1234"
  })
});
```

</Request>

</Requests>

<Responses items={["200","400","401","403","404","409","429","500"]}>

<Response value={"200"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "keyId": "key_1234",
  "valid": true,
  "name": "Customer X",
  "ownerId": "user_123",
  "meta": {
    "roles": [
      "admin",
      "user"
    ],
    "stripeCustomerId": "cus_1234"
  },
  "expires": 123,
  "ratelimit": {
    "limit": 10,
    "remaining": 9,
    "reset": 3600000
  },
  "remaining": 1000,
  "code": "NOT_FOUND",
  "enabled": true
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  /**
   * The id of the key
   */
  keyId?: string;
  /**
   * Whether the key is valid or not.
   * A key could be invalid for a number of reasons, for example if it has expired, has no more verifications left or if it has been deleted.
   */
  valid: boolean;
  /**
   * The name of the key, give keys a name to easily identifiy their purpose
   */
  name?: string;
  /**
   * The id of the tenant associated with this key. Use whatever reference you have in your system to identify the tenant. When verifying the key, we will send this field back to you, so you know who is accessing your API.
   */
  ownerId?: string;
  /**
   * Any additional metadata you want to store with the key
   */
  meta?: {
    [k: string]: {
      [k: string]: unknown;
    };
  };
  /**
   * The unix timestamp in milliseconds when the key will expire. If this field is null or undefined, the key is not expiring.
   */
  expires?: number;
  /**
   * The ratelimit configuration for this key. If this field is null or undefined, the key has no ratelimit.
   */
  ratelimit?: {
    /**
     * Maximum number of requests that can be made inside a window
     */
    limit: number;
    /**
     * Remaining requests after this verification
     */
    remaining: number;
    /**
     * Unix timestamp in milliseconds when the ratelimit will reset
     */
    reset: number;
  };
  /**
   * The number of requests that can be made with this key before it becomes invalid. If this field is null or undefined, the key has no request limit.
   */
  remaining?: number;
  /**
   * If the key is invalid this field will be set to the reason why it is invalid.
   * Possible values are:
   * - NOT_FOUND: the key does not exist or has expired
   * - FORBIDDEN: the key is not allowed to access the api
   * - USAGE_EXCEEDED: the key has exceeded its request limit
   * - RATE_LIMITED: the key has been ratelimited
   * - UNAUTHORIZED: the key is not authorized
   * - DISABLED: the key is disabled
   * - INSUFFICIENT_PERMISSIONS: you do not have the required permissions to perform this action
   *
   */
  code?:
    | "NOT_FOUND"
    | "FORBIDDEN"
    | "USAGE_EXCEEDED"
    | "RATE_LIMITED"
    | "UNAUTHORIZED"
    | "DISABLED"
    | "INSUFFICIENT_PERMISSIONS";
  /**
   * Sets the key to be enabled or disabled. Disabled keys will not verify.
   */
  enabled?: boolean;
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"400"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "error": {
    "code": "BAD_REQUEST",
    "docs": "https://unkey.dev/docs/api-reference/errors/code/BAD_REQUEST",
    "message": "string",
    "requestId": "req_1234"
  }
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  error: {
    /**
     * A machine readable error code.
     */
    code: "BAD_REQUEST";
    /**
     * A link to our documentation with more details about this error code
     */
    docs: string;
    /**
     * A human readable explanation of what went wrong
     */
    message: string;
    /**
     * Please always include the requestId in your error report
     */
    requestId: string;
  };
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"401"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "error": {
    "code": "UNAUTHORIZED",
    "docs": "https://unkey.dev/docs/api-reference/errors/code/UNAUTHORIZED",
    "message": "string",
    "requestId": "req_1234"
  }
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  error: {
    /**
     * A machine readable error code.
     */
    code: "UNAUTHORIZED";
    /**
     * A link to our documentation with more details about this error code
     */
    docs: string;
    /**
     * A human readable explanation of what went wrong
     */
    message: string;
    /**
     * Please always include the requestId in your error report
     */
    requestId: string;
  };
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"403"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "error": {
    "code": "FORBIDDEN",
    "docs": "https://unkey.dev/docs/api-reference/errors/code/FORBIDDEN",
    "message": "string",
    "requestId": "req_1234"
  }
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  error: {
    /**
     * A machine readable error code.
     */
    code: "FORBIDDEN";
    /**
     * A link to our documentation with more details about this error code
     */
    docs: string;
    /**
     * A human readable explanation of what went wrong
     */
    message: string;
    /**
     * Please always include the requestId in your error report
     */
    requestId: string;
  };
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"404"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "error": {
    "code": "NOT_FOUND",
    "docs": "https://unkey.dev/docs/api-reference/errors/code/NOT_FOUND",
    "message": "string",
    "requestId": "req_1234"
  }
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  error: {
    /**
     * A machine readable error code.
     */
    code: "NOT_FOUND";
    /**
     * A link to our documentation with more details about this error code
     */
    docs: string;
    /**
     * A human readable explanation of what went wrong
     */
    message: string;
    /**
     * Please always include the requestId in your error report
     */
    requestId: string;
  };
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"409"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "error": {
    "code": "CONFLICT",
    "docs": "https://unkey.dev/docs/api-reference/errors/code/CONFLICT",
    "message": "string",
    "requestId": "req_1234"
  }
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  error: {
    /**
     * A machine readable error code.
     */
    code: "CONFLICT";
    /**
     * A link to our documentation with more details about this error code
     */
    docs: string;
    /**
     * A human readable explanation of what went wrong
     */
    message: string;
    /**
     * Please always include the requestId in your error report
     */
    requestId: string;
  };
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"429"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "error": {
    "code": "TOO_MANY_REQUESTS",
    "docs": "https://unkey.dev/docs/api-reference/errors/code/TOO_MANY_REQUESTS",
    "message": "string",
    "requestId": "req_1234"
  }
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  error: {
    /**
     * A machine readable error code.
     */
    code: "TOO_MANY_REQUESTS";
    /**
     * A link to our documentation with more details about this error code
     */
    docs: string;
    /**
     * A human readable explanation of what went wrong
     */
    message: string;
    /**
     * Please always include the requestId in your error report
     */
    requestId: string;
  };
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"500"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "error": {
    "code": "INTERNAL_SERVER_ERROR",
    "docs": "https://unkey.dev/docs/api-reference/errors/code/INTERNAL_SERVER_ERROR",
    "message": "string",
    "requestId": "req_1234"
  }
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  error: {
    /**
     * A machine readable error code.
     */
    code: "INTERNAL_SERVER_ERROR";
    /**
     * A link to our documentation with more details about this error code
     */
    docs: string;
    /**
     * A human readable explanation of what went wrong
     */
    message: string;
    /**
     * Please always include the requestId in your error report
     */
    requestId: string;
  };
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

</Responses>

</APIExample>

</API>

<API>

<APIInfo method={"POST"} route={"/v1/keys.updateKey"}>

## Update Key

<APIPlayground authorization={{"type":"string","name":"Authorization","defaultValue":"Bearer","isRequired":true,"description":"The Authorization access token"}} method={"POST"} route={"/v1/keys.updateKey"} bodyType={"json"} path={[]} query={[]} header={[]} body={{"type":"object","isRequired":true,"properties":{"keyId":{"type":"ref","isRequired":true,"schema":"0"},"name":{"type":"ref","isRequired":false,"schema":"1"},"ownerId":{"type":"ref","isRequired":false,"schema":"2"},"meta":{"type":"ref","isRequired":false,"schema":"3"},"expires":{"type":"ref","isRequired":false,"schema":"4"},"ratelimit":{"type":"ref","isRequired":false,"schema":"5"},"remaining":{"type":"ref","isRequired":false,"schema":"10"},"refill":{"type":"ref","isRequired":false,"schema":"11"},"enabled":{"type":"ref","isRequired":false,"schema":"14"}}}} schemas={{"0":{"type":"string","defaultValue":"key_123","isRequired":false,"description":"The id of the key you want to modify"},"1":{"type":"string","defaultValue":"Customer X","isRequired":false,"description":"The name of the key"},"2":{"type":"string","defaultValue":"user_123","isRequired":false,"description":"The id of the tenant associated with this key. Use whatever reference you have in your system to identify the tenant. When verifying the key, we will send this field back to you, so you know who is accessing your API."},"3":{"type":"object","isRequired":false,"description":"Any additional metadata you want to store with the key","properties":{},"additionalProperties":true},"4":{"type":"number","defaultValue":0,"isRequired":false,"description":"The unix timestamp in milliseconds when the key will expire. If this field is null or undefined, the key is not expiring."},"5":{"type":"object","isRequired":false,"description":"Unkey comes with per-key ratelimiting out of the box. Set `null` to disable.","properties":{"type":{"type":"ref","isRequired":true,"schema":"6"},"limit":{"type":"ref","isRequired":true,"schema":"7"},"refillRate":{"type":"ref","isRequired":true,"schema":"8"},"refillInterval":{"type":"ref","isRequired":true,"schema":"9"}}},"6":{"type":"string","defaultValue":"","isRequired":false,"description":"Fast ratelimiting doesn't add latency, while consistent ratelimiting is more accurate."},"7":{"type":"number","defaultValue":"","isRequired":false,"description":"The total amount of burstable requests."},"8":{"type":"number","defaultValue":"","isRequired":false,"description":"How many tokens to refill during each refillInterval."},"9":{"type":"number","defaultValue":"","isRequired":false,"description":"Determines the speed at which tokens are refilled, in milliseconds."},"10":{"type":"number","defaultValue":1000,"isRequired":false,"description":"The number of requests that can be made with this key before it becomes invalid. Set `null` to disable."},"11":{"type":"object","isRequired":false,"description":"Unkey enables you to refill verifications for each key at regular intervals.","properties":{"interval":{"type":"ref","isRequired":true,"schema":"12"},"amount":{"type":"ref","isRequired":true,"schema":"13"}}},"12":{"type":"string","defaultValue":"","isRequired":false,"description":"Unkey will automatically refill verifications at the set interval. If null is used the refill functionality will be removed from the key."},"13":{"type":"number","defaultValue":"","isRequired":false,"description":"The amount of verifications to refill for each occurrence is determined individually for each key."},"14":{"type":"boolean","defaultValue":true,"isRequired":false,"description":"Set if key is enabled or disabled. If disabled, the key cannot be used to verify."}}}>

</APIPlayground>

### Authorization

<Property name={"Authorization"} type={"Bearer <token>"} required={true}>

In: `header`

</Property>

### Request Body

<Property name={"keyId"} type={"string"} required={true} deprecated={undefined}>

The id of the key you want to modify

<span>Example: `"key_123"`</span>

</Property>

<Property name={"name"} type={"string | null"} required={false} deprecated={undefined}>

The name of the key

<span>Example: `"Customer X"`</span>

</Property>

<Property name={"ownerId"} type={"string | null"} required={false} deprecated={undefined}>

The id of the tenant associated with this key. Use whatever reference you have in your system to identify the tenant. When verifying the key, we will send this field back to you, so you know who is accessing your API.

<span>Example: `"user_123"`</span>

</Property>

<Property name={"meta"} type={"object | null"} required={false} deprecated={undefined}>

Any additional metadata you want to store with the key

<span>Example: `{"roles":["admin","user"],"stripeCustomerId":"cus_1234"}`</span>

<ObjectCollapsible name={"Attributes"}>

<Property name={"[key: string]"} type={"null"} required={false} deprecated={undefined}>

</Property>

</ObjectCollapsible>

</Property>

<Property name={"expires"} type={"number | null"} required={false} deprecated={undefined}>

The unix timestamp in milliseconds when the key will expire. If this field is null or undefined, the key is not expiring.

<span>Example: `0`</span>

</Property>

<Property name={"ratelimit"} type={"object | null"} required={false} deprecated={undefined}>

Unkey comes with per-key ratelimiting out of the box. Set `null` to disable.

<span>Example: `{"type":"fast","limit":10,"refillRate":1,"refillInterval":60}`</span>

<ObjectCollapsible name={"Attributes"}>

<Property name={"type"} type={"string"} required={true} deprecated={undefined}>

Fast ratelimiting doesn't add latency, while consistent ratelimiting is more accurate.

<span>Value in: `"fast" | "consistent"`</span>

</Property>

<Property name={"limit"} type={"integer"} required={true} deprecated={undefined}>

The total amount of burstable requests.

<span>Minimum: `1`</span>

</Property>

<Property name={"refillRate"} type={"integer"} required={true} deprecated={undefined}>

How many tokens to refill during each refillInterval.

<span>Minimum: `1`</span>

</Property>

<Property name={"refillInterval"} type={"integer"} required={true} deprecated={undefined}>

Determines the speed at which tokens are refilled, in milliseconds.

<span>Minimum: `1`</span>

</Property>

</ObjectCollapsible>

</Property>

<Property name={"remaining"} type={"number | null"} required={false} deprecated={undefined}>

The number of requests that can be made with this key before it becomes invalid. Set `null` to disable.

<span>Example: `1000`</span>

</Property>

<Property name={"refill"} type={"object | null"} required={false} deprecated={undefined}>

Unkey enables you to refill verifications for each key at regular intervals.

<span>Example: `{"interval":"daily","amount":100}`</span>

<ObjectCollapsible name={"Attributes"}>

<Property name={"interval"} type={"string"} required={true} deprecated={undefined}>

Unkey will automatically refill verifications at the set interval. If null is used the refill functionality will be removed from the key.

<span>Value in: `"daily" | "monthly"`</span>

</Property>

<Property name={"amount"} type={"integer"} required={true} deprecated={undefined}>

The amount of verifications to refill for each occurrence is determined individually for each key.

<span>Minimum: `1`</span>

</Property>

</ObjectCollapsible>

</Property>

<Property name={"enabled"} type={"boolean"} required={false} deprecated={undefined}>

Set if key is enabled or disabled. If disabled, the key cannot be used to verify.

<span>Example: `true`</span>

</Property>

| Status code | Description |
| ----------- | ----------- |
| `200` | The key was successfully updated, it may take up to 30s for this to take effect in all regions |
| `400` | The server cannot or will not process the request due to something that is perceived to be a client error (e.g., malformed request syntax, invalid request message framing, or deceptive request routing). |
| `401` | Although the HTTP standard specifies "unauthorized", semantically this response means "unauthenticated". That is, the client must authenticate itself to get the requested response. |
| `403` | The client does not have access rights to the content; that is, it is unauthorized, so the server is refusing to give the requested resource. Unlike 401 Unauthorized, the client's identity is known to the server. |
| `404` | The server cannot find the requested resource. In the browser, this means the URL is not recognized. In an API, this can also mean that the endpoint is valid but the resource itself does not exist. Servers may also send this response instead of 403 Forbidden to hide the existence of a resource from an unauthorized client. This response code is probably the most well known due to its frequent occurrence on the web. |
| `409` | This response is sent when a request conflicts with the current state of the server. |
| `429` | The user has sent too many requests in a given amount of time ("rate limiting") |
| `500` | The server has encountered a situation it does not know how to handle. |

</APIInfo>

<APIExample>

<Requests items={["cURL","JavaScript"]}>

<Request value={"cURL"}>

```bash
curl -X POST "https://api.unkey.dev/v1/keys.updateKey" \
  -H "Content-Type: application/json" \
  -d '{
    "keyId": "key_123",
    "name": "Customer X",
    "ownerId": "user_123",
    "meta": {
      "roles": [
        "admin",
        "user"
      ],
      "stripeCustomerId": "cus_1234"
    },
    "expires": 0,
    "ratelimit": {
      "type": "fast",
      "limit": 10,
      "refillRate": 1,
      "refillInterval": 60
    },
    "remaining": 1000,
    "refill": {
      "interval": "daily",
      "amount": 100
    },
    "enabled": true
  }'
```

</Request>

<Request value={"JavaScript"}>

```js
fetch("https://api.unkey.dev/v1/keys.updateKey", {
  method: "POST",
  body: JSON.stringify({
    "keyId": "key_123",
    "name": "Customer X",
    "ownerId": "user_123",
    "meta": {
      "roles": [
        "admin",
        "user"
      ],
      "stripeCustomerId": "cus_1234"
    },
    "expires": 0,
    "ratelimit": {
      "type": "fast",
      "limit": 10,
      "refillRate": 1,
      "refillInterval": 60
    },
    "remaining": 1000,
    "refill": {
      "interval": "daily",
      "amount": 100
    },
    "enabled": true
  })
});
```

</Request>

</Requests>

<Responses items={["200","400","401","403","404","409","429","500"]}>

<Response value={"200"}>

<ResponseTypes>

<ExampleResponse>

```json
{}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"400"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "error": {
    "code": "BAD_REQUEST",
    "docs": "https://unkey.dev/docs/api-reference/errors/code/BAD_REQUEST",
    "message": "string",
    "requestId": "req_1234"
  }
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  error: {
    /**
     * A machine readable error code.
     */
    code: "BAD_REQUEST";
    /**
     * A link to our documentation with more details about this error code
     */
    docs: string;
    /**
     * A human readable explanation of what went wrong
     */
    message: string;
    /**
     * Please always include the requestId in your error report
     */
    requestId: string;
  };
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"401"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "error": {
    "code": "UNAUTHORIZED",
    "docs": "https://unkey.dev/docs/api-reference/errors/code/UNAUTHORIZED",
    "message": "string",
    "requestId": "req_1234"
  }
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  error: {
    /**
     * A machine readable error code.
     */
    code: "UNAUTHORIZED";
    /**
     * A link to our documentation with more details about this error code
     */
    docs: string;
    /**
     * A human readable explanation of what went wrong
     */
    message: string;
    /**
     * Please always include the requestId in your error report
     */
    requestId: string;
  };
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"403"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "error": {
    "code": "FORBIDDEN",
    "docs": "https://unkey.dev/docs/api-reference/errors/code/FORBIDDEN",
    "message": "string",
    "requestId": "req_1234"
  }
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  error: {
    /**
     * A machine readable error code.
     */
    code: "FORBIDDEN";
    /**
     * A link to our documentation with more details about this error code
     */
    docs: string;
    /**
     * A human readable explanation of what went wrong
     */
    message: string;
    /**
     * Please always include the requestId in your error report
     */
    requestId: string;
  };
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"404"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "error": {
    "code": "NOT_FOUND",
    "docs": "https://unkey.dev/docs/api-reference/errors/code/NOT_FOUND",
    "message": "string",
    "requestId": "req_1234"
  }
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  error: {
    /**
     * A machine readable error code.
     */
    code: "NOT_FOUND";
    /**
     * A link to our documentation with more details about this error code
     */
    docs: string;
    /**
     * A human readable explanation of what went wrong
     */
    message: string;
    /**
     * Please always include the requestId in your error report
     */
    requestId: string;
  };
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"409"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "error": {
    "code": "CONFLICT",
    "docs": "https://unkey.dev/docs/api-reference/errors/code/CONFLICT",
    "message": "string",
    "requestId": "req_1234"
  }
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  error: {
    /**
     * A machine readable error code.
     */
    code: "CONFLICT";
    /**
     * A link to our documentation with more details about this error code
     */
    docs: string;
    /**
     * A human readable explanation of what went wrong
     */
    message: string;
    /**
     * Please always include the requestId in your error report
     */
    requestId: string;
  };
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"429"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "error": {
    "code": "TOO_MANY_REQUESTS",
    "docs": "https://unkey.dev/docs/api-reference/errors/code/TOO_MANY_REQUESTS",
    "message": "string",
    "requestId": "req_1234"
  }
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  error: {
    /**
     * A machine readable error code.
     */
    code: "TOO_MANY_REQUESTS";
    /**
     * A link to our documentation with more details about this error code
     */
    docs: string;
    /**
     * A human readable explanation of what went wrong
     */
    message: string;
    /**
     * Please always include the requestId in your error report
     */
    requestId: string;
  };
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"500"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "error": {
    "code": "INTERNAL_SERVER_ERROR",
    "docs": "https://unkey.dev/docs/api-reference/errors/code/INTERNAL_SERVER_ERROR",
    "message": "string",
    "requestId": "req_1234"
  }
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  error: {
    /**
     * A machine readable error code.
     */
    code: "INTERNAL_SERVER_ERROR";
    /**
     * A link to our documentation with more details about this error code
     */
    docs: string;
    /**
     * A human readable explanation of what went wrong
     */
    message: string;
    /**
     * Please always include the requestId in your error report
     */
    requestId: string;
  };
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

</Responses>

</APIExample>

</API>

<API>

<APIInfo method={"POST"} route={"/v1/keys.updateRemaining"}>

## Update Remaining

<APIPlayground authorization={{"type":"string","name":"Authorization","defaultValue":"Bearer","isRequired":true,"description":"The Authorization access token"}} method={"POST"} route={"/v1/keys.updateRemaining"} bodyType={"json"} path={[]} query={[]} header={[]} body={{"type":"object","isRequired":true,"properties":{"keyId":{"type":"ref","isRequired":true,"schema":"0"},"op":{"type":"ref","isRequired":true,"schema":"1"},"value":{"type":"ref","isRequired":true,"schema":"2"}}}} schemas={{"0":{"type":"string","defaultValue":"key_123","isRequired":false,"description":"The id of the key you want to modify"},"1":{"type":"string","defaultValue":"","isRequired":false,"description":"The operation you want to perform on the remaining count"},"2":{"type":"number","defaultValue":1,"isRequired":false,"description":"The value you want to set, add or subtract the remaining count by"}}}>

</APIPlayground>

### Authorization

<Property name={"Authorization"} type={"Bearer <token>"} required={true}>

In: `header`

</Property>

### Request Body

<Property name={"keyId"} type={"string"} required={true} deprecated={undefined}>

The id of the key you want to modify

<span>Example: `"key_123"`</span>

</Property>

<Property name={"op"} type={"string"} required={true} deprecated={undefined}>

The operation you want to perform on the remaining count

<span>Value in: `"increment" | "decrement" | "set"`</span>

</Property>

<Property name={"value"} type={"integer | null"} required={true} deprecated={undefined}>

The value you want to set, add or subtract the remaining count by

<span>Example: `1`</span>

</Property>

| Status code | Description |
| ----------- | ----------- |
| `200` | The configuration for an api |
| `400` | The server cannot or will not process the request due to something that is perceived to be a client error (e.g., malformed request syntax, invalid request message framing, or deceptive request routing). |
| `401` | Although the HTTP standard specifies "unauthorized", semantically this response means "unauthenticated". That is, the client must authenticate itself to get the requested response. |
| `403` | The client does not have access rights to the content; that is, it is unauthorized, so the server is refusing to give the requested resource. Unlike 401 Unauthorized, the client's identity is known to the server. |
| `404` | The server cannot find the requested resource. In the browser, this means the URL is not recognized. In an API, this can also mean that the endpoint is valid but the resource itself does not exist. Servers may also send this response instead of 403 Forbidden to hide the existence of a resource from an unauthorized client. This response code is probably the most well known due to its frequent occurrence on the web. |
| `409` | This response is sent when a request conflicts with the current state of the server. |
| `429` | The user has sent too many requests in a given amount of time ("rate limiting") |
| `500` | The server has encountered a situation it does not know how to handle. |

</APIInfo>

<APIExample>

<Requests items={["cURL","JavaScript"]}>

<Request value={"cURL"}>

```bash
curl -X POST "https://api.unkey.dev/v1/keys.updateRemaining" \
  -H "Content-Type: application/json" \
  -d '{
    "keyId": "key_123",
    "op": "increment",
    "value": 1
  }'
```

</Request>

<Request value={"JavaScript"}>

```js
fetch("https://api.unkey.dev/v1/keys.updateRemaining", {
  method: "POST",
  body: JSON.stringify({
    "keyId": "key_123",
    "op": "increment",
    "value": 1
  })
});
```

</Request>

</Requests>

<Responses items={["200","400","401","403","404","409","429","500"]}>

<Response value={"200"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "remaining": 100
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  /**
   * The number of remaining requests for this key after updating it. `null` means unlimited.
   */
  remaining: number;
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"400"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "error": {
    "code": "BAD_REQUEST",
    "docs": "https://unkey.dev/docs/api-reference/errors/code/BAD_REQUEST",
    "message": "string",
    "requestId": "req_1234"
  }
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  error: {
    /**
     * A machine readable error code.
     */
    code: "BAD_REQUEST";
    /**
     * A link to our documentation with more details about this error code
     */
    docs: string;
    /**
     * A human readable explanation of what went wrong
     */
    message: string;
    /**
     * Please always include the requestId in your error report
     */
    requestId: string;
  };
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"401"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "error": {
    "code": "UNAUTHORIZED",
    "docs": "https://unkey.dev/docs/api-reference/errors/code/UNAUTHORIZED",
    "message": "string",
    "requestId": "req_1234"
  }
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  error: {
    /**
     * A machine readable error code.
     */
    code: "UNAUTHORIZED";
    /**
     * A link to our documentation with more details about this error code
     */
    docs: string;
    /**
     * A human readable explanation of what went wrong
     */
    message: string;
    /**
     * Please always include the requestId in your error report
     */
    requestId: string;
  };
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"403"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "error": {
    "code": "FORBIDDEN",
    "docs": "https://unkey.dev/docs/api-reference/errors/code/FORBIDDEN",
    "message": "string",
    "requestId": "req_1234"
  }
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  error: {
    /**
     * A machine readable error code.
     */
    code: "FORBIDDEN";
    /**
     * A link to our documentation with more details about this error code
     */
    docs: string;
    /**
     * A human readable explanation of what went wrong
     */
    message: string;
    /**
     * Please always include the requestId in your error report
     */
    requestId: string;
  };
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"404"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "error": {
    "code": "NOT_FOUND",
    "docs": "https://unkey.dev/docs/api-reference/errors/code/NOT_FOUND",
    "message": "string",
    "requestId": "req_1234"
  }
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  error: {
    /**
     * A machine readable error code.
     */
    code: "NOT_FOUND";
    /**
     * A link to our documentation with more details about this error code
     */
    docs: string;
    /**
     * A human readable explanation of what went wrong
     */
    message: string;
    /**
     * Please always include the requestId in your error report
     */
    requestId: string;
  };
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"409"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "error": {
    "code": "CONFLICT",
    "docs": "https://unkey.dev/docs/api-reference/errors/code/CONFLICT",
    "message": "string",
    "requestId": "req_1234"
  }
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  error: {
    /**
     * A machine readable error code.
     */
    code: "CONFLICT";
    /**
     * A link to our documentation with more details about this error code
     */
    docs: string;
    /**
     * A human readable explanation of what went wrong
     */
    message: string;
    /**
     * Please always include the requestId in your error report
     */
    requestId: string;
  };
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"429"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "error": {
    "code": "TOO_MANY_REQUESTS",
    "docs": "https://unkey.dev/docs/api-reference/errors/code/TOO_MANY_REQUESTS",
    "message": "string",
    "requestId": "req_1234"
  }
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  error: {
    /**
     * A machine readable error code.
     */
    code: "TOO_MANY_REQUESTS";
    /**
     * A link to our documentation with more details about this error code
     */
    docs: string;
    /**
     * A human readable explanation of what went wrong
     */
    message: string;
    /**
     * Please always include the requestId in your error report
     */
    requestId: string;
  };
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"500"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "error": {
    "code": "INTERNAL_SERVER_ERROR",
    "docs": "https://unkey.dev/docs/api-reference/errors/code/INTERNAL_SERVER_ERROR",
    "message": "string",
    "requestId": "req_1234"
  }
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  error: {
    /**
     * A machine readable error code.
     */
    code: "INTERNAL_SERVER_ERROR";
    /**
     * A link to our documentation with more details about this error code
     */
    docs: string;
    /**
     * A human readable explanation of what went wrong
     */
    message: string;
    /**
     * Please always include the requestId in your error report
     */
    requestId: string;
  };
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

</Responses>

</APIExample>

</API>

<API>

<APIInfo method={"GET"} route={"/v1/keys.getVerifications"}>

## Get Verifications

<APIPlayground authorization={{"type":"string","name":"Authorization","defaultValue":"Bearer","isRequired":true,"description":"The Authorization access token"}} method={"GET"} route={"/v1/keys.getVerifications"} bodyType={"json"} path={[]} query={[{"name":"keyId","type":"string","defaultValue":"key_1234","isRequired":false,"description":"The id of the key to fetch, either `keyId` or `ownerId` must be provided"},{"name":"ownerId","type":"string","defaultValue":"chronark","isRequired":false,"description":"The owner id to fetch keys for, either `keyId` or `ownerId` must be provided"},{"name":"start","type":"number","defaultValue":1620000000000,"isRequired":false,"description":"The start of the period to fetch usage for as unix milliseconds timestamp"},{"name":"end","type":"number","defaultValue":1620000000000,"isRequired":false,"description":"The end of the period to fetch usage for as unix milliseconds timestamp"},{"name":"granularity","type":"string","defaultValue":"day","isRequired":false,"description":"The granularity of the usage data to fetch, currently only `day` is supported"}]} header={[]} body={undefined} schemas={{}}>

</APIPlayground>

### Authorization

<Property name={"Authorization"} type={"Bearer <token>"} required={true}>

In: `header`

</Property>

### Query Parameters

<Property name={"keyId"} type={"string"} required={false} deprecated={false}>

The id of the key to fetch, either `keyId` or `ownerId` must be provided

<span>Example: `"key_1234"`</span>

</Property>

<Property name={"ownerId"} type={"string"} required={false} deprecated={false}>

The owner id to fetch keys for, either `keyId` or `ownerId` must be provided

<span>Example: `"chronark"`</span>

</Property>

<Property name={"start"} type={"integer | null"} required={false} deprecated={false}>

The start of the period to fetch usage for as unix milliseconds timestamp

<span>Example: `1620000000000`</span>

</Property>

<Property name={"end"} type={"integer | null"} required={false} deprecated={false}>

The end of the period to fetch usage for as unix milliseconds timestamp

<span>Example: `1620000000000`</span>

</Property>

<Property name={"granularity"} type={"string"} required={false} deprecated={false}>

The granularity of the usage data to fetch, currently only `day` is supported

<span>Example: `"day"`</span>

<span>Default: `"day"`</span>

<span>Value in: `"day"`</span>

</Property>

| Status code | Description |
| ----------- | ----------- |
| `200` | The configuration for a single key |
| `400` | The server cannot or will not process the request due to something that is perceived to be a client error (e.g., malformed request syntax, invalid request message framing, or deceptive request routing). |
| `401` | Although the HTTP standard specifies "unauthorized", semantically this response means "unauthenticated". That is, the client must authenticate itself to get the requested response. |
| `403` | The client does not have access rights to the content; that is, it is unauthorized, so the server is refusing to give the requested resource. Unlike 401 Unauthorized, the client's identity is known to the server. |
| `404` | The server cannot find the requested resource. In the browser, this means the URL is not recognized. In an API, this can also mean that the endpoint is valid but the resource itself does not exist. Servers may also send this response instead of 403 Forbidden to hide the existence of a resource from an unauthorized client. This response code is probably the most well known due to its frequent occurrence on the web. |
| `409` | This response is sent when a request conflicts with the current state of the server. |
| `429` | The user has sent too many requests in a given amount of time ("rate limiting") |
| `500` | The server has encountered a situation it does not know how to handle. |

</APIInfo>

<APIExample>

<Requests items={["cURL","JavaScript"]}>

<Request value={"cURL"}>

```bash
curl -X GET "https://api.unkey.dev/v1/keys.getVerifications?keyId=key_1234&ownerId=chronark&start=1620000000000&end=1620000000000&granularity=day"
```

</Request>

<Request value={"JavaScript"}>

```js
fetch("https://api.unkey.dev/v1/keys.getVerifications?keyId=key_1234&ownerId=chronark&start=1620000000000&end=1620000000000&granularity=day", {
  method: "GET"
});
```

</Request>

</Requests>

<Responses items={["200","400","401","403","404","409","429","500"]}>

<Response value={"200"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "verifications": [
    {
      "time": 1620000000000,
      "success": 100,
      "rateLimited": 10,
      "usageExceeded": 0
    }
  ]
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  verifications: {
    /**
     * The timestamp of the usage data
     */
    time: number;
    /**
     * The number of successful requests
     */
    success: number;
    /**
     * The number of requests that were rate limited
     */
    rateLimited: number;
    /**
     * The number of requests that exceeded the usage limit
     */
    usageExceeded: number;
  }[];
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"400"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "error": {
    "code": "BAD_REQUEST",
    "docs": "https://unkey.dev/docs/api-reference/errors/code/BAD_REQUEST",
    "message": "string",
    "requestId": "req_1234"
  }
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  error: {
    /**
     * A machine readable error code.
     */
    code: "BAD_REQUEST";
    /**
     * A link to our documentation with more details about this error code
     */
    docs: string;
    /**
     * A human readable explanation of what went wrong
     */
    message: string;
    /**
     * Please always include the requestId in your error report
     */
    requestId: string;
  };
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"401"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "error": {
    "code": "UNAUTHORIZED",
    "docs": "https://unkey.dev/docs/api-reference/errors/code/UNAUTHORIZED",
    "message": "string",
    "requestId": "req_1234"
  }
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  error: {
    /**
     * A machine readable error code.
     */
    code: "UNAUTHORIZED";
    /**
     * A link to our documentation with more details about this error code
     */
    docs: string;
    /**
     * A human readable explanation of what went wrong
     */
    message: string;
    /**
     * Please always include the requestId in your error report
     */
    requestId: string;
  };
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"403"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "error": {
    "code": "FORBIDDEN",
    "docs": "https://unkey.dev/docs/api-reference/errors/code/FORBIDDEN",
    "message": "string",
    "requestId": "req_1234"
  }
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  error: {
    /**
     * A machine readable error code.
     */
    code: "FORBIDDEN";
    /**
     * A link to our documentation with more details about this error code
     */
    docs: string;
    /**
     * A human readable explanation of what went wrong
     */
    message: string;
    /**
     * Please always include the requestId in your error report
     */
    requestId: string;
  };
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"404"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "error": {
    "code": "NOT_FOUND",
    "docs": "https://unkey.dev/docs/api-reference/errors/code/NOT_FOUND",
    "message": "string",
    "requestId": "req_1234"
  }
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  error: {
    /**
     * A machine readable error code.
     */
    code: "NOT_FOUND";
    /**
     * A link to our documentation with more details about this error code
     */
    docs: string;
    /**
     * A human readable explanation of what went wrong
     */
    message: string;
    /**
     * Please always include the requestId in your error report
     */
    requestId: string;
  };
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"409"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "error": {
    "code": "CONFLICT",
    "docs": "https://unkey.dev/docs/api-reference/errors/code/CONFLICT",
    "message": "string",
    "requestId": "req_1234"
  }
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  error: {
    /**
     * A machine readable error code.
     */
    code: "CONFLICT";
    /**
     * A link to our documentation with more details about this error code
     */
    docs: string;
    /**
     * A human readable explanation of what went wrong
     */
    message: string;
    /**
     * Please always include the requestId in your error report
     */
    requestId: string;
  };
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"429"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "error": {
    "code": "TOO_MANY_REQUESTS",
    "docs": "https://unkey.dev/docs/api-reference/errors/code/TOO_MANY_REQUESTS",
    "message": "string",
    "requestId": "req_1234"
  }
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  error: {
    /**
     * A machine readable error code.
     */
    code: "TOO_MANY_REQUESTS";
    /**
     * A link to our documentation with more details about this error code
     */
    docs: string;
    /**
     * A human readable explanation of what went wrong
     */
    message: string;
    /**
     * Please always include the requestId in your error report
     */
    requestId: string;
  };
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

<Response value={"500"}>

<ResponseTypes>

<ExampleResponse>

```json
{
  "error": {
    "code": "INTERNAL_SERVER_ERROR",
    "docs": "https://unkey.dev/docs/api-reference/errors/code/INTERNAL_SERVER_ERROR",
    "message": "string",
    "requestId": "req_1234"
  }
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
export interface Response {
  error: {
    /**
     * A machine readable error code.
     */
    code: "INTERNAL_SERVER_ERROR";
    /**
     * A link to our documentation with more details about this error code
     */
    docs: string;
    /**
     * A human readable explanation of what went wrong
     */
    message: string;
    /**
     * Please always include the requestId in your error report
     */
    requestId: string;
  };
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

</Responses>

</APIExample>

</API>

</Root>
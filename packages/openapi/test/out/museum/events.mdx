---
title: Events
description: Special events hosted by the Museum
---

import { Root, API, APIInfo, APIExample, Responses, Response, ResponseTypes, ExampleResponse, TypeScriptResponse, Property, ObjectCollapsible } from "fumadocs-ui/components/api";

<Root>

<API>

<APIInfo method={"GET"} route={"/special-events"}>

## List special events

Return a list of upcoming special events at the museum.

### Query Parameters

<Property name={"startDate"} type={"string"} required={false} deprecated={false}>

The starting date to retrieve future operating hours from. Defaults to today's date.

<span>Example:  `"2023-02-23"`</span>

<span>Format:  `"date"`</span>

</Property>

<Property name={"endDate"} type={"string"} required={false} deprecated={false}>

The end of a date range to retrieve special events for. Defaults to 7 days after `startDate`.

<span>Example:  `"2023-04-18"`</span>

<span>Format:  `"date"`</span>

</Property>

<Property name={"page"} type={"integer"} required={false} deprecated={false}>

The page number to retrieve.

<span>Example:  `2`</span>

<span>Default:  `1`</span>

</Property>

<Property name={"limit"} type={"integer"} required={false} deprecated={false}>

The number of days per page.

<span>Example:  `15`</span>

<span>Default:  `10`</span>

<span>Maximum:  `30`</span>

</Property>

| Status code | Description |
| ----------- | ----------- |
| `200` | Success |
| `400` | Bad request |
| `404` | Not found |

</APIInfo>

<APIExample>

```bash title="curl"
curl -X GET "https://api.fake-museum-example.com/special-events"
```

<Responses items={["200"]}>

<Response value={"200"}>

A list of upcoming special events

<ResponseTypes>

<ExampleResponse>

```json
[
  {
    "eventId": "3be6453c-03eb-4357-ae5a-984a0e574a54",
    "name": "Pirate Coding Workshop",
    "location": "Computer Room",
    "eventDescription": "Captain Blackbeard shares his love of the C...language. And possibly Arrrrr (R lang).",
    "dates": [
      "2023-10-29"
    ],
    "price": 25
  }
]
```

</ExampleResponse>

<TypeScriptResponse>

```ts
/**
 * A list of upcoming special events
 */
export type Response = {
  /**
   * Identifier for a special event.
   */
  eventId: string;
  /**
   * Name of the special event
   */
  name: string;
  /**
   * Location where the special event is held
   */
  location: string;
  /**
   * Description of the special event
   */
  eventDescription: string;
  /**
   * List of planned dates for the special event
   */
  dates: string[];
  /**
   * Price of a ticket for the special event
   */
  price: number;
}[];
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

</Responses>

</APIExample>

</API>

<API>

<APIInfo method={"POST"} route={"/special-events"}>

## Create special events

Creates a new special event for the museum.

### Request Body

<Property name={"name"} type={"string"} required={true} deprecated={undefined}>

Name of the special event

<span>Example:  `"Pirate Coding Workshop"`</span>

</Property>

<Property name={"location"} type={"string"} required={true} deprecated={undefined}>

Location where the special event is held

<span>Example:  `"Computer Room"`</span>

</Property>

<Property name={"eventDescription"} type={"string"} required={true} deprecated={undefined}>

Description of the special event

<span>Example:  `"Captain Blackbeard shares his love of the C...language. And possibly Arrrrr (R lang)."`</span>

</Property>

<Property name={"dates"} type={"array of string"} required={true} deprecated={undefined}>

List of planned dates for the special event

</Property>

<Property name={"price"} type={"number"} required={true} deprecated={undefined}>

Price of a ticket for the special event

<span>Example:  `25`</span>

<span>Format:  `"float"`</span>

</Property>

| Status code | Description |
| ----------- | ----------- |
| `200` | Success |
| `400` | Bad request |
| `404` | Not found |

</APIInfo>

<APIExample>

```bash title="curl"
curl -X POST "https://api.fake-museum-example.com/special-events" \
  -d '{
  "name": "Pirate Coding Workshop",
  "location": "Computer Room",
  "eventDescription": "Captain Blackbeard shares his love of the C...language. And possibly Arrrrr (R lang).",
  "dates": [
    "2023-10-29"
  ],
  "price": 25
}'
```

<Responses items={["200"]}>

<Response value={"200"}>

Information about a special event.

<ResponseTypes>

<ExampleResponse>

```json
{
  "eventId": "3be6453c-03eb-4357-ae5a-984a0e574a54",
  "name": "Pirate Coding Workshop",
  "location": "Computer Room",
  "eventDescription": "Captain Blackbeard shares his love of the C...language. And possibly Arrrrr (R lang).",
  "dates": [
    "2023-10-29"
  ],
  "price": 25
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
/**
 * Information about a special event.
 */
export interface Response {
  /**
   * Identifier for a special event.
   */
  eventId: string;
  /**
   * Name of the special event
   */
  name: string;
  /**
   * Location where the special event is held
   */
  location: string;
  /**
   * Description of the special event
   */
  eventDescription: string;
  /**
   * List of planned dates for the special event
   */
  dates: string[];
  /**
   * Price of a ticket for the special event
   */
  price: number;
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

</Responses>

</APIExample>

</API>

<API>

<APIInfo method={"GET"} route={"/special-events/{eventId}"}>

## Get special event

Get details about a special event.

### Path Parameters

<Property name={"eventId"} type={"string"} required={true} deprecated={false}>

An identifier for a special event.

<span>Example:  `"dad4bce8-f5cb-4078-a211-995864315e39"`</span>

<span>Format:  `"uuid"`</span>

</Property>

| Status code | Description |
| ----------- | ----------- |
| `200` | Success |
| `400` | Bad request |
| `404` | Not found |

</APIInfo>

<APIExample>

```bash title="curl"
curl -X GET "https://api.fake-museum-example.com/special-events/dad4bce8-f5cb-4078-a211-995864315e39"
```

<Responses items={["200"]}>

<Response value={"200"}>

Information about a special event.

<ResponseTypes>

<ExampleResponse>

```json
{
  "eventId": "3be6453c-03eb-4357-ae5a-984a0e574a54",
  "name": "Pirate Coding Workshop",
  "location": "Computer Room",
  "eventDescription": "Captain Blackbeard shares his love of the C...language. And possibly Arrrrr (R lang).",
  "dates": [
    "2023-10-29"
  ],
  "price": 25
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
/**
 * Information about a special event.
 */
export interface Response {
  /**
   * Identifier for a special event.
   */
  eventId: string;
  /**
   * Name of the special event
   */
  name: string;
  /**
   * Location where the special event is held
   */
  location: string;
  /**
   * Description of the special event
   */
  eventDescription: string;
  /**
   * List of planned dates for the special event
   */
  dates: string[];
  /**
   * Price of a ticket for the special event
   */
  price: number;
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

</Responses>

</APIExample>

</API>

<API>

<APIInfo method={"PATCH"} route={"/special-events/{eventId}"}>

## Update special event

Update the details of a special event

### Request Body

<Property name={"name"} type={"string"} required={false} deprecated={undefined}>

Name of the special event

<span>Example:  `"Pirate Coding Workshop"`</span>

</Property>

<Property name={"location"} type={"string"} required={false} deprecated={undefined}>

Location where the special event is held

<span>Example:  `"Computer Room"`</span>

</Property>

<Property name={"eventDescription"} type={"string"} required={false} deprecated={undefined}>

Description of the special event

<span>Example:  `"Captain Blackbeard shares his love of the C...language. And possibly Arrrrr (R lang)."`</span>

</Property>

<Property name={"dates"} type={"array of string"} required={false} deprecated={undefined}>

List of planned dates for the special event

</Property>

<Property name={"price"} type={"number"} required={false} deprecated={undefined}>

Price of a ticket for the special event

<span>Example:  `25`</span>

<span>Format:  `"float"`</span>

</Property>

### Path Parameters

<Property name={"eventId"} type={"string"} required={true} deprecated={false}>

An identifier for a special event.

<span>Example:  `"dad4bce8-f5cb-4078-a211-995864315e39"`</span>

<span>Format:  `"uuid"`</span>

</Property>

| Status code | Description |
| ----------- | ----------- |
| `200` | Success |
| `400` | Bad request |
| `404` | Not found |

</APIInfo>

<APIExample>

```bash title="curl"
curl -X PATCH "https://api.fake-museum-example.com/special-events/dad4bce8-f5cb-4078-a211-995864315e39" \
  -d '{
  "name": "Pirate Coding Workshop",
  "location": "Computer Room",
  "eventDescription": "Captain Blackbeard shares his love of the C...language. And possibly Arrrrr (R lang).",
  "dates": [
    "2023-10-29"
  ],
  "price": 25
}'
```

<Responses items={["200"]}>

<Response value={"200"}>

Information about a special event.

<ResponseTypes>

<ExampleResponse>

```json
{
  "eventId": "3be6453c-03eb-4357-ae5a-984a0e574a54",
  "name": "Pirate Coding Workshop",
  "location": "Computer Room",
  "eventDescription": "Captain Blackbeard shares his love of the C...language. And possibly Arrrrr (R lang).",
  "dates": [
    "2023-10-29"
  ],
  "price": 25
}
```

</ExampleResponse>

<TypeScriptResponse>

```ts
/**
 * Information about a special event.
 */
export interface Response {
  /**
   * Identifier for a special event.
   */
  eventId: string;
  /**
   * Name of the special event
   */
  name: string;
  /**
   * Location where the special event is held
   */
  location: string;
  /**
   * Description of the special event
   */
  eventDescription: string;
  /**
   * List of planned dates for the special event
   */
  dates: string[];
  /**
   * Price of a ticket for the special event
   */
  price: number;
}
```

</TypeScriptResponse>

</ResponseTypes>

</Response>

</Responses>

</APIExample>

</API>

<API>

<APIInfo method={"DELETE"} route={"/special-events/{eventId}"}>

## Delete special event

Delete a special event from the collection. Allows museum to cancel planned events.

### Path Parameters

<Property name={"eventId"} type={"string"} required={true} deprecated={false}>

An identifier for a special event.

<span>Example:  `"dad4bce8-f5cb-4078-a211-995864315e39"`</span>

<span>Format:  `"uuid"`</span>

</Property>

| Status code | Description |
| ----------- | ----------- |
| `204` | Success - no content |
| `400` | Bad request |
| `401` | Unauthorized |
| `404` | Not found |

</APIInfo>

<APIExample>

```bash title="curl"
curl -X DELETE "https://api.fake-museum-example.com/special-events/dad4bce8-f5cb-4078-a211-995864315e39"
```

</APIExample>

</API>

</Root>
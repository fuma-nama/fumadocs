export default function withArticle(props) {
  return (
    <main {...props} className="w-full max-w-[1400px] mx-auto px-4 py-12">
      <article className="prose">{props.children}</article>
    </main>
  );
}

## Hello World

Create a `page.mdx` file, put the page under a non-docs layout.

Apply typography styles with:

```jsx
export default function withArticle(props) {
  return (
    <main {...props} className={cn('w-full max-w-[1400px] mx-auto px-4 py-12', props.className)}>
      <article className="prose">{props.children}</article>
    </main>
  );
}
```

Done!

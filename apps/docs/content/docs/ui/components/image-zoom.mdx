---
title: Zoom Image
description: Allow zoom-in images in your documentation
preview: zoom-image
---

> it is an optional component which won't be loaded by default.

## Usage

Add it to your MDX components.

> A default `sizes` property will be defined if not specified

```tsx
import { ImageZoom } from '@fuma-docs/ui/components/image-zoom';

const components = {
  img: (props) => <ImageZoom {...props} />,
};
```

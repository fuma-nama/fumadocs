---
title: Layout
description: The layout of documentation
icon: LayoutIcon
---

import { TypeTable } from 'next-docs-ui/components/type-table'

The layout of documentation pages, it must be nested under the
[Root Provider](/docs/ui/blocks/root-provider).

## Usage

Wrap the children and pass your page tree to the component.

```jsx
import { DocsLayout } from 'next-docs-ui/layout'

export default function Layout({ children }) {
  return <DocsLayout tree={tree}>{children}</DocsLayout>
}
```

<TypeTable
  type={{
    navTitle: { description: 'Navbar title', type: 'ReactNode | string' },
    navItems: { description: 'Navbar items', type: 'NavItemProps[]' },
    tree: { type: 'PageTree' },
    nav: {
      type: 'ReactNode | false',
      description: 'Replace or disable default navbar'
    },
    githubUrl: {
      type: 'string',
      description: 'Github url displayed on the navbar'
    },
    sidebar: {
      type: 'ReactNode | false',
      description: 'Disable or replace default sidebar'
    },
    sidebarCollapsible: {
      type: 'boolean',
      description: 'Enable collapsing sidebar',
      default: 'true'
    },
    sidebarDefaultOpenLevel: {
      type: 'number',
      description:
        'Open folders by default if their level is lower or equal to a specific level (Starting from 1)',
      default: '1'
    },
    sidebarBanner: {
      type: 'ReactNode',
      description: 'Content at the top of sidebar'
    },
    sidebarFooter: {
      type: 'ReactNode',
      description: 'Content at the bottom of sidebar'
    },
    children: { type: 'ReactNode' }
  }}
/>

### Disabling Navbar

It is common to share a navbar across all the pages.

To achieve this, you may disable the default navbar by passing `false` to the
`nav` prop.

The default navbar is exported from `next-docs-ui/component`, you can add it to
the root layout (under the root provider).

<br />

<Accordion type="single" collapsible>
<AccordionItem value="custom-navbar">
<AccordionTrigger>Custom Navbar</AccordionTrigger>
<AccordionContent>

Example for custom navbar using Tailwind CSS.

```tsx
'use client'

import { GithubIcon } from 'lucide-react'
import { Nav as OriginalNav } from 'next-docs-ui/components'
import Link from 'next/link'
import { usePathname } from 'next/navigation'

export function Nav() {
  const pathname = usePathname()
  const inDocs = pathname.startsWith('/docs/') || pathname === '/docs'

  return (
    <OriginalNav
      enableSidebar={inDocs}
      links={[
        {
          icon: <GithubIcon className="h-5 w-5" />,
          href: 'https://github.com/SonMooSans/next-docs',
          external: true
        }
      ]}
    >
      <Link
        href="/"
        className="hover:text-muted-foreground whitespace-nowrap font-semibold"
      >
        Next Docs
      </Link>
    </OriginalNav>
  )
}
```

</AccordionContent>
</AccordionItem>
</Accordion>

### Example

```tsx title="app/docs/[[...slug]]/layout.tsx"
import { DocsLayout } from 'next-docs-ui/layout'
import { ReactNode } from 'react'

export default function Layout({ children }: { children: ReactNode }) {
  return <DocsLayout tree={tree}>{children}</DocsLayout>
}
```
